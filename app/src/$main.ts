import React = require("react");
import ReactDOM = require("react-dom");
import ReactRouter = require("react-router");
import login = require("login");

namespace main {
    // export const React = __React;
    // export const login = __login;
}